---
tags:
  - 개발환경
  - AndroidStudio
  - 실행방법
관련:
  - "[[03_기술_스택]]"
  - "[[02_시스템_아키텍처]]"
---

# 08. 개발환경 설정

> **최종 업데이트**: 2026-04 (Java 전환)

---

## 🛠️ 사전 요구사항

| 도구 | 버전 | 설치 확인 |
|---|---|---|
| **Android Studio** | Ladybug+ (최신) | Android Studio → About |
| **JDK** | 17 LTS | `java -version` |
| **Android SDK** | API 35 (Target), API 26 (Min) | SDK Manager |
| **Git** | 최신 | `git --version` |

> [!tip] Gemini Nano 테스트
> Gemini Nano는 Pixel 8 Pro+ 또는 Samsung Galaxy S24+ 실기기에서만 테스트 가능.
> 에뮬레이터에서는 `AiSummaryRepository.isAvailable()`이 `false`를 반환한다.

---

## 📂 프로젝트 디렉토리 구조

```
moneylog/
├── app/
│   └── src/main/
│       ├── java/com/moneylog/
│       │   ├── data/
│       │   │   ├── db/
│       │   │   │   ├── AppDatabase.java              # Room DB (v3), 기본 카테고리 시드
│       │   │   │   ├── dao/
│       │   │   │   │   ├── TransactionDao.java
│       │   │   │   │   ├── CategoryDao.java
│       │   │   │   │   ├── RecurringDao.java
│       │   │   │   │   ├── MonthlySummary.java
│       │   │   │   │   ├── DailySummary.java
│       │   │   │   │   └── CategorySummary.java
│       │   │   │   └── entity/
│       │   │   │       ├── TransactionEntity.java
│       │   │   │       ├── CategoryEntity.java
│       │   │   │       └── RecurringEntity.java
│       │   │   ├── repository/
│       │   │   │   ├── TransactionRepository.java
│       │   │   │   ├── CategoryRepository.java
│       │   │   │   ├── RecurringRepository.java
│       │   │   │   ├── BackupRepository.java          # Google Drive 백업·복원
│       │   │   │   └── AiSummaryRepository.java       # Gemini Nano 분석
│       │   │   └── worker/
│       │   │       └── RecurringWorker.java           # @HiltWorker, 매일 1회
│       │   ├── ui/
│       │   │   ├── fragment/
│       │   │   │   ├── OnboardingFragment.java        # 언어 선택 (최초 실행)
│       │   │   │   ├── DashboardFragment.java
│       │   │   │   ├── TransactionFragment.java
│       │   │   │   ├── TransactionFormFragment.java
│       │   │   │   ├── StatisticsFragment.java
│       │   │   │   ├── AiSummaryFragment.java
│       │   │   │   ├── CategoryFragment.java
│       │   │   │   └── SettingsFragment.java
│       │   │   ├── adapter/
│       │   │   │   ├── TransactionAdapter.java
│       │   │   │   ├── CategoryAdapter.java
│       │   │   │   └── IconPickerAdapter.java
│       │   │   ├── viewmodel/
│       │   │   │   ├── TransactionViewModel.java
│       │   │   │   ├── DashboardViewModel.java
│       │   │   │   ├── StatisticsViewModel.java
│       │   │   │   ├── RecurringViewModel.java
│       │   │   │   ├── CategoryViewModel.java
│       │   │   │   └── AiSummaryViewModel.java
│       │   │   └── widget/
│       │   │       └── PieChartView.java
│       │   ├── di/
│       │   │   └── AppModule.java                     # Hilt @Module
│       │   ├── util/
│       │   │   ├── DateUtils.java
│       │   │   ├── FormatUtils.java
│       │   │   ├── LocaleHelper.java                  # ko/en/ja 언어 전환
│       │   │   ├── IconHelper.java                    # Material Icons 매핑
│       │   │   ├── DataManagementHelper.java          # CSV, 데이터 정리
│       │   │   └── YearMonthPickerDialog.java
│       │   ├── MainActivity.java
│       │   └── MoneyLogApplication.java
│       ├── res/
│       │   ├── layout/           # activity_main, fragment_*, item_*, dialog_*, bottom_sheet_* XML
│       │   ├── navigation/       # nav_graph.xml
│       │   ├── menu/             # bottom_nav_menu.xml
│       │   ├── color/            # bottom_nav_selector.xml
│       │   ├── drawable/         # ic_*.xml, bg_*.xml (카테고리 아이콘 포함)
│       │   ├── font/             # pretendard_*.ttf, manrope_*.ttf
│       │   ├── values/           # colors.xml, strings.xml, themes.xml
│       │   ├── values-en/        # strings.xml (English)
│       │   ├── values-ja/        # strings.xml (日本語)
│       │   └── xml/              # backup_rules.xml, data_extraction_rules.xml, locales_config.xml
│       └── AndroidManifest.xml
├── build.gradle.kts              # 프로젝트 레벨
├── app/build.gradle.kts          # 앱 모듈 레벨 (annotationProcessor, viewBinding)
├── gradle/libs.versions.toml     # 버전 카탈로그
└── README.md
```

---

## 🚀 실행 순서

### 1단계: 프로젝트 클론

```bash
git clone https://github.com/your-username/moneylog.git
cd moneylog
```

### 2단계: Android Studio에서 열기

1. Android Studio → `File → Open` → 프로젝트 폴더 선택
2. Gradle Sync 완료 대기

### 3단계: 실행

- **에뮬레이터**: API 26+ 기기 생성 → Run
- **실기기**: USB 디버깅 활성화 → 연결 → Run

> [!tip] 확인
> 앱이 실행되면 언어 선택 온보딩 화면이 표시된다.

---

## ⚙️ Google API 설정

### Google Cloud Console

1. [Google Cloud Console](https://console.cloud.google.com/)에서 프로젝트 생성
2. **Google Drive API** 활성화
3. **OAuth 2.0 클라이언트 ID** 생성 (Android 유형)
   - 패키지 이름: `com.moneylog`
   - SHA-1 지문:

     ```bash
     keytool -keystore ~/.android/debug.keystore -list -v -alias androiddebugkey -storepass android
     ```

4. 동의 화면 구성 (scope: `drive.appdata`)

### Gemini Nano 설정

1. `build.gradle.kts`에 Google AI Edge SDK 의존성 추가
2. `AndroidManifest.xml`에 AICore 관련 메타 데이터 추가:

   ```xml
   <meta-data
       android:name="com.google.android.aicore"
       android:value="gemini-nano" />
   ```

3. 런타임에 `isAvailable()` 체크 → 미지원 시 AI 기능 숨김

---

## 📦 주요 의존성

### `gradle/libs.versions.toml` (핵심 항목)

```toml
[versions]
agp              = "8.9.0"
appcompat        = "1.7.0"
material         = "1.12.0"
navigation       = "2.8.9"
lifecycle        = "2.8.7"
room             = "2.7.1"
hilt             = "2.56"
work-runtime     = "2.10.1"
desugar-jdk-libs = "2.1.5"

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
hilt                = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }

[libraries]
# Jetpack Core
appcompat            = { group = "androidx.appcompat",     name = "appcompat",         version.ref = "appcompat" }
material             = { group = "com.google.android.material", name = "material",      version.ref = "material"  }
navigation-fragment  = { group = "androidx.navigation",   name = "navigation-fragment", version.ref = "navigation" }
navigation-ui        = { group = "androidx.navigation",   name = "navigation-ui",       version.ref = "navigation" }

# Room (annotationProcessor)
room-runtime  = { group = "androidx.room", name = "room-runtime",  version.ref = "room" }
room-compiler = { group = "androidx.room", name = "room-compiler",  version.ref = "room" }

# Hilt (annotationProcessor)
hilt-android  = { group = "com.google.dagger", name = "hilt-android",  version.ref = "hilt" }
hilt-compiler = { group = "com.google.dagger", name = "hilt-compiler",  version.ref = "hilt" }

junit-android-ext = { group = "androidx.test.ext", name = "junit", version.ref = "junit-android" }
espresso-core     = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espresso" }
room-testing      = { group = "androidx.room", name = "room-testing", version.ref = "room" }
```

---

## 🧪 테스트 실행

```bash
# 단위 테스트
./gradlew test

# Instrumented 테스트 (에뮬레이터/실기기 필요)
./gradlew connectedAndroidTest

# 특정 테스트 클래스 실행
./gradlew test --tests "com.moneylog.data.repository.TransactionRepositoryTest"
```

---

## 🚀 빌드 & 배포

### Debug APK 빌드

```bash
./gradlew assembleDebug
# 출력: app/build/outputs/apk/debug/app-debug.apk
```

### Release AAB 빌드 (Play Store)

```bash
./gradlew bundleRelease
# 출력: app/build/outputs/bundle/release/app-release.aab
```

### 서명 설정 (`app/build.gradle.kts`)

```kotlin
android {
    signingConfigs {
        create("release") {
            storeFile = file(System.getenv("KEYSTORE_PATH") ?: "keystore.jks")
            storePassword = System.getenv("KEYSTORE_PASSWORD") ?: ""
            keyAlias = System.getenv("KEY_ALIAS") ?: ""
            keyPassword = System.getenv("KEY_PASSWORD") ?: ""
        }
    }
}
```

### Play Store 배포

1. [Google Play Console](https://play.google.com/console/) 개발자 등록 ($25 일회성)
2. 앱 생성 → AAB 업로드 → 내부 테스트 → 공개

---

## 🔗 연관 문서

- [[02_시스템_아키텍처]] — 시스템 구조
- [[03_기술_스택]] — 기술 스택 상세

### 스택: #개발환경 #AndroidStudio #Gradle #PlayStore
